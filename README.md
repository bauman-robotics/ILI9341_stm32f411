# ILI9341_STM32F411 - Полнофункциональная QWERTY виртуальная клавиатура

Проект демонстрирует работу полнофункциональной виртуальной QWERTY клавиатуры на TFT дисплее ILI9341 с микроконтроллером STM32F411CEU6. Реализованы продвинутые оптимизации производительности, USB логирование и отладочные инструменты.

## 🎯 Возможности

- **Полнофункциональная QWERTY клавиатура** с 4 рядами букв и цифрами
- **6 функциональных кнопок** с пиктограммами (⇧ □ A↔ × ↲ ≡)
- **Поддержка тачскрина MSP2807** с SPI2 и прерываниями
- **Полноценный драйвер ILI9341** с landscape ориентацией (320x240)
- **Два рабочих шрифта**: Font1 (5x7) и Font1_2x (10x14)
- **USB CDC логирование** с отладкой координат и тачскрина
- **Конфигурационная система** для разных режимов работы
- **Оптимизированная производительность** с double buffering и DMA
- **VSCode интеграция** с горячими клавишами
- **Подробная документация** всех решений и проблем

## 🚀 Быстрый старт

### Требования
- **STM32F411CEU6** (Black Pill или аналог)
- **ILI9341 TFT дисплей** (320x240, SPI интерфейс)
- **arm-none-eabi-gcc** toolchain
- **make** для сборки
- **st-flash** для прошивки (опционально)

### Подключение
```
STM32F411          ILI9341
PA5 (SPI1_SCK) ──► SCK
PA6 (SPI1_MISO) ──► (не используется)
PA7 (SPI1_MOSI) ──► SDA/MOSI
PB0              ──► CS
PB1              ──► RST
PA8              ──► DC
PC14             ──► LED (подсветка)
GND              ──► GND
3.3V             ──► VCC
```

### Сборка и прошивка

```bash
# Клонировать репозиторий
git clone git@github.com:bauman-robotics/ILI9341_stm32f411.git
cd ILI9341_STM32F411

# Собрать проект
make

# Прошить через ST-Link
make flash

# Или собрать и прошить одной командой
make all-flash
```

## 📋 Режимы работы

Проект поддерживает три режима через конфигурационный файл `Core/Inc/config.h`:

### 1. Режим алфавита (`TASK_ALPHABET_DISPLAY`)
```c
#define TASK_ALPHABET_DISPLAY 1
#define TASK_SCROLLING_HELLO 0
#define TASK_QWERTY_KEYBOARD 0
```
**Отображает:**
- Полный алфавит A-Z и a-z
- Два шрифта одновременно
- Статическое отображение для тестирования

### 2. Режим бегущей строки (`TASK_SCROLLING_HELLO`)
```c
#define TASK_ALPHABET_DISPLAY 0
#define TASK_SCROLLING_HELLO 1
#define TASK_QWERTY_KEYBOARD 0
```
**Отображает:**
- Плавную анимацию "Hello World! "
- Крупный шрифт (масштаб 2x)
- Без мерцания (double buffering)

### 3. QWERTY клавиатура (`TASK_QWERTY_KEYBOARD`)
```c
#define TASK_ALPHABET_DISPLAY 0
#define TASK_SCROLLING_HELLO 0
#define TASK_QWERTY_KEYBOARD 1
#define KEYBOARD_PALETTE KEYBOARD_PALETTE_CLASSIC  // или DARK/MODERN
```
**Отображает:**
- Полную QWERTY раскладку с цифрами и функциональными кнопками
- **4 ряда основных клавиш:** 1-0, QWERTYUIOP, ASDFGHJKL, ZXCVBNM., (включая знаки препинания)
- **6 функциональных кнопок:** ⇧ (Caps), □ (Space), A↔ (Lang), × (Cancel), ↲ (Enter), ≡ (Menu)
- Три цветовые палитры: CLASSIC (белый/голубой), DARK (серый/темно-синий), MODERN (черный/розовый)
- Каждая клавиша в рамке для touchscreen интеграции
- Поле ввода текста в верхней части экрана
- USB логирование всех координат элементов (ENABLE_FRAMEBUFFER_DEBUG)

## 🏗️ Архитектура

### Double Buffering
Проект использует продвинутую технику double buffering для устранения мерцания:

```
Font1 Data ──▶ Software Buffer ──▶ ILI9341 SPI
  (5x7)         (320x30 pixels)    (38,400 bytes)
```

- **Software framebuffer**: 19,200 байт RAM
- **Отрисовка в памяти**: Быстрые операции без SPI
- **Единичная передача**: Полный кадр за один SPI вызов
- **Результат**: 50 FPS без мерцания

### Оптимизации производительности

1. **Умное частичное стирание** - только необходимые области
2. **DMA для больших передач** - ускорение SPI операций
3. **Оптимизированные задержки** - 20ms для плавной анимации
4. **Эффективное управление памятью** - динамические буферы

## 📁 Структура проекта

```
ILI9341_STM32F411/
├── Core/
│   ├── Inc/
│   │   ├── config.h          # Конфигурация режимов
│   │   ├── fonts.h           # Шрифты и заголовки
│   │   ├── ili9341.h         # Драйвер дисплея
│   │   ├── logger.h          # USB CDC логирование
│   │   └── main.h            # Основные заголовки
│   └── Src/
│       ├── fonts.c           # Реализация шрифтов
│       ├── freertos.c        # FreeRTOS задачи
│       ├── ili9341.c         # Драйвер ILI9341
│       ├── logger.c          # USB логирование
│       └── main.c            # Точка входа
├── Drivers/                  # HAL драйверы STM32
├── Middlewares/              # FreeRTOS, USB
├── Info/                     # Документация проекта
│   ├── 01_pinouts.md         # Распиновка
│   ├── 03_plan.md           # План разработки
│   ├── 04_tools.md          # Инструментарий
│   ├── 05_fonts.md          # Информация о шрифтах
│   ├── 06_status.md         # Состояние проекта
│   └── 07_display_optimization.md # Оптимизации
├── .vscode/                  # VSCode конфигурация
├── Makefile                  # Скрипт сборки
├── STM32F411CEUx_FLASH.ld    # Linker script
├── startup_stm32f411xe.s     # Startup код
└── README.md                 # Этот файл
```

## 🔧 Сборка и разработка

### VSCode интеграция
Проект настроен для комфортной разработки в VSCode:
- **Tasks**: Build, Clean, Flash, Build & Flash
- **Горячие клавиши**: Ctrl+Shift+B (сборка), Ctrl+Shift+F (прошивка)
- **IntelliSense**: Полная поддержка автодополнения

### Команды сборки
```bash
make            # Собрать проект
make clean      # Очистить build artifacts
make flash      # Прошить через ST-Link
make all-flash  # Собрать и прошить
```

## 📊 Технические характеристики

- **Микроконтроллер**: STM32F411CEU6 (100MHz Cortex-M4)
- **Дисплей**: ILI9341 320x240 RGB565 с виртуальной клавиатурой
- **Интерфейс**: SPI1 (36MHz max)
- **ОС**: FreeRTOS 10.4.6
- **Память**: ~75KB бинарник, 19KB RAM буфер
- **Производительность**: 50 FPS анимация без мерцания
- **Клавиатура**: 40+ интерактивных элементов с пиктограммами

## 🔍 Отладочные инструменты

Проект включает мощные инструменты для отладки интерфейса:

### Отладка координат фреймбуфера
```c
#define ENABLE_FRAMEBUFFER_DEBUG 1  // В config.h
```
**Логирует в USB CDC:**
- Размеры дисплея и всех элементов
- Координаты каждого ряда клавиатуры
- Позиции функциональных кнопок
- Схематичное ASCII представление интерфейса

**Пример вывода:**
```
=== FRAMEBUFFER COORDINATES DEBUG ===
DISPLAY: Width=320, Height=240
TEXT INPUT FIELD: X=5, Y=5, Width=310, Height=60
NUMBERS ROW: Y=70, Height=28, Keys: 10
QWERTY ROW: Y=105, Height=28, Keys: 10
ASDF ROW: Y=138, Height=28, Keys: 9
ZXCV ROW: Y=171, Height=28, Keys: 9
FUNCTIONAL BUTTONS: Y=220, Height=20, Count=6
SCHEMATIC LAYOUT:
┌─────────────────────────────────────┐
│ ┌─────────────────────────────────┐ │
│ │        TEXT INPUT FIELD         │ │ Y=5-65
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
     ↓ 5px
┌───┬───┬───┬───┬───┬───┬───┬───┬───┬───┐ Y=70
│ 1 │ 2 │ 3 │ 4 │ 5 │ 6 │ 7 │ 8 │ 9 │ 0 │
└───┴───┴───┴───┴───┴───┴───┴───┴───┴───┘
=== END FRAMEBUFFER DEBUG ===
```

### USB CDC логирование
- **Инициализация ILI9341** с подробными этапами
- **Координаты элементов** для отладки позиционирования
- **Логи тачскрина** с координатами нажатий
- **Чистый вывод** без артефактов SPI
- **Retry логика** при отправке данных

### Отладка тачскрина
```c
#define ENABLE_TOUCH_DEBUG 1  // В config.h
```
**Логирует в USB CDC:**
- Координаты касаний (X, Y)
- Давление нажатия (0-4095)
- Тип события (PRESS, RELEASE, MOVE)
- Временные метки

**Пример вывода:**
```
TOUCH: X=150, Y=200, Pressure=3500, Event=1
TOUCH: X=145, Y=195, Pressure=3200, Event=2
```

## 🐛 Устранение неполадок

### Дисплей не показывает ничего
1. Проверить подключение питания и подсветки
2. Проверить SPI пины (PA5, PA7, PB0, PA8)
3. Попробовать режим алфавита для диагностики

### Мерцание при анимации
- В проекте реализован double buffering для устранения мерцания
- Если мерцание все же есть, проверить частоту SPI

### Проблемы с компиляцией
```bash
# Очистить и пересобрать
make clean && make

# Проверить toolchain
arm-none-eabi-gcc --version
```

## 🤝 Вклад в проект

Проект открыт для улучшений! Возможные направления:

- **Touch драйвер** для MSP2807
- **Кириллические шрифты**
- **Графические примитивы** (линии, круги)
- **DMA оптимизации** для всего экрана
- **Множественные буферы** для сложной анимации

## 📄 Лицензия

Этот проект предоставляется как есть, без каких-либо гарантий. Используйте на свой страх и риск.

## 📞 Контакты

Для вопросов и предложений создавайте issues в репозитории.

---

**Приятного использования ILI9341 с STM32F411!** 🎉
