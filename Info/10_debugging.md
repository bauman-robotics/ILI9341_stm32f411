# Руководство по отладке STM32F411 + ILI9341

## 📋 Содержание

1. [Введение](#введение)
2. [Подготовка к отладке](#подготовка-к-отладке)
3. [VSCode отладка](#vscode-отладка)
4. [Командная строка](#командная-строка)
5. [Диагностика TOUCH_Init()](#диагностика-touch_init)
6. [Распространенные проблемы](#распространенные-проблемы)
7. [Продвинутая отладка](#продвинутая-отладка)

---

## 🎯 Введение

Этот документ содержит полную инструкцию по отладке проекта ILI9341_STM32F411. Проект включает несколько уровней отладки:

- **USB CDC логирование** - программные логи в реальном времени
- **Логический анализатор** - аппаратные сигналы GPIO/SPI
- **OpenOCD + GDB** - пошаговая отладка микроконтроллера

### Основная проблема для диагностики

**TOUCH_Init() вызывает зависание системы** - CDC не поднимается, нет изображения на экране. Нужно найти причину зависания в функции инициализации тачскрина.

---

## 🔧 Подготовка к отладке

### Требуемое оборудование

1. **ST-Link V2 или выше** (обязательно)
2. **STM32F411 Black Pill** (или аналог)
3. **USB-UART адаптер** (для логов, опционально)
4. **Логический анализатор** (для сигналов, опционально)

### Подключение ST-Link

```
ST-Link     STM32F411
SWDIO   ──► PA13
SWCLK   ──► PA14
GND     ──► GND
3.3V    ──► 3.3V (опционально)
```

### Установка ПО

#### Ubuntu/Debian:
```bash
# OpenOCD
sudo apt update
sudo apt install openocd

# ARM toolchain (если не установлен)
sudo apt install gcc-arm-none-eabi gdb-multiarch

# VSCode extensions
code --install-extension marus25.cortex-debug
code --install-extension ms-vscode.cpptools
```

#### Arch Linux:
```bash
sudo pacman -S openocd arm-none-eabi-gcc arm-none-eabi-gdb
```

#### macOS:
```bash
brew install openocd
# ARM toolchain установить отдельно
```

### Проверка установки

```bash
# OpenOCD
openocd --version

# GDB
arm-none-eabi-gdb --version

# Сборка проекта
make clean && make
```

---

## 🐛 VSCode отладка

### Настройка VSCode

1. **Установите расширения:**
   - `Cortex-Debug` (marus25.cortex-debug)
   - `C/C++` (ms-vscode.cpptools)

2. **Откройте проект в VSCode**

3. **Проверьте файлы конфигурации:**
   - `.vscode/launch.json` - конфигурация отладки
   - `.vscode/tasks.json` - задачи сборки
   - `.vscode/settings.json` - настройки

### Запуск отладки

#### Способ 1: Кнопки в Status Bar

1. **Подключите ST-Link**
2. **Нажмите ▶️ Debug** в нижней панели VSCode**
3. **Выберите конфигурацию:** "Debug STM32F411 (OpenOCD)"
4. **Проект автоматически соберется и запустится отладка**

#### Способ 2: F5

1. **Нажмите F5**
2. **Выберите:** "Debug STM32F411 (OpenOCD)"

### Интерфейс отладки

#### Основные кнопки:
- **▶️ Continue** - продолжить выполнение
- **⏸️ Pause** - приостановить
- **🔄 Restart** - перезапустить
- **⏹️ Stop** - остановить отладку

#### Панели:
- **Variables** - локальные переменные
- **Watch** - наблюдаемые выражения
- **Call Stack** - стек вызовов
- **Breakpoints** - точки останова
- **Disassembly** - дизассемблер

### Установка breakpoints

#### В коде:
```c
// Click в поле слева от номера строки
TOUCH_Init();  // Красная точка появится
```

#### В отладчике:
```gdb
break TOUCH_Init
break main
break MX_FREERTOS_Init
```

### Пошаговая отладка

1. **Остановитесь на breakpoint**
2. **F10 (Step Over)** - выполнить строку, не заходя в функции
3. **F11 (Step Into)** - зайти в функцию
4. **Shift+F11 (Step Out)** - выйти из функции
5. **F5 (Continue)** - продолжить до следующего breakpoint

---

## 💻 Командная строка

### Автоматический скрипт

```bash
# Сделать скрипт исполняемым
chmod +x debug.sh

# Запустить отладку
./debug.sh
```

Скрипт автоматически:
- Проверит наличие OpenOCD и GDB
- Запустит OpenOCD
- Подключит GDB
- Выполнит базовые команды

### Ручная настройка (если скрипт не работает)

```bash
# Терминал 1: OpenOCD
openocd -f openocd.cfg

# Терминал 2: GDB
arm-none-eabi-gdb build/ILI9341_stm32f411.elf
(gdb) target remote localhost:3333
(gdb) monitor reset halt
(gdb) break main
(gdb) continue
```

### Ручная отладка

#### Терминал 1 - OpenOCD:
```bash
make debug
# или
openocd -f openocd.cfg
```

#### Терминал 2 - GDB:
```bash
arm-none-eabi-gdb build/ILI9341_stm32f411.elf

# Подключиться к OpenOCD
(gdb) target remote localhost:3333

# Сбросить и остановить
(gdb) monitor reset halt

# Установить breakpoints
(gdb) break main
(gdb) break TOUCH_Init

# Запустить
(gdb) continue
```

### Telnet интерфейс OpenOCD

```bash
# Подключиться к OpenOCD
telnet localhost 4444

# Команды:
reset halt          # Сброс и остановка
flash probe 0       # Проверить flash
mdw 0x08000000 10   # Прочитать память
mwb 0x20000000 0xAA # Записать в RAM
```

---

## 🔍 Диагностика TOUCH_Init()

### Проблема

Функция `TOUCH_Init()` вызывает зависание системы. Нужно найти, какая именно строка вызывает проблему.

### Шаг 1: Установка breakpoints

```gdb
# В GDB
break TOUCH_Init
run

# Программа остановится в начале TOUCH_Init()
```

### Шаг 2: Пошаговое выполнение

```gdb
# Выполнять по одной команде
step

# Следить за:
# - Изменением регистров
# - Выполнением HAL функций
# - Значением переменных
```

### Шаг 3: Анализ HAL функций

```c
// TOUCH_Init() содержит:

// 1. GPIO настройка
__HAL_RCC_GPIOB_CLK_ENABLE();
GPIOB->MODER |= GPIO_MODER_MODER13_0;
GPIOB->BSRR = GPIO_PIN_13;

// 2. HAL GPIO для прерываний
HAL_GPIO_Init(GPIOB, &GPIO_InitStruct);  // <-- Возможная проблема

// 3. Прерывания
HAL_NVIC_EnableIRQ(EXTI9_5_IRQn);        // <-- Возможная проблема

// 4. SPI2 инициализация
HAL_SPI_Init(&hspi2);                    // <-- Возможная проблема
```

### Шаг 4: Тестирование по частям

#### Тест 1: Только GPIO
```c
// Оставить только GPIO код, закомментировать HAL
TOUCH_Init() {
    __HAL_RCC_GPIOB_CLK_ENABLE();
    GPIOB->MODER |= GPIO_MODER_MODER13_0;
    GPIOB->BSRR = GPIO_PIN_13;
    // Без HAL_GPIO_Init, HAL_NVIC_EnableIRQ, HAL_SPI_Init
}
```

#### Тест 2: Добавить HAL GPIO
```c
// Добавить HAL_GPIO_Init
HAL_GPIO_Init(GPIOB, &GPIO_InitStruct);
```

#### Тест 3: Добавить прерывания
```c
// Добавить HAL_NVIC_EnableIRQ
HAL_NVIC_EnableIRQ(EXTI9_5_IRQn);
```

#### Тест 4: Добавить SPI2
```c
// Добавить HAL_SPI_Init
HAL_SPI_Init(&hspi2);
```

### Шаг 5: Анализ регистров

```gdb
# После зависания
info registers    # Состояние регистров
backtrace         # Стек вызовов
info threads      # Состояние потоков
```

---

## 🚨 Распространенные проблемы

### VSCode: Настройка отладки

#### Проблема: Кнопки отладки не появляются
**Решение:**
1. Установите Cortex-Debug extension
2. Перезагрузите VSCode: `Ctrl+Shift+P → "Developer: Reload Window"`
3. Проверьте файлы конфигурации:
   ```bash
   ls -la .vscode/
   cat .vscode/launch.json
   ```
4. Добавьте в settings.json:
   ```json
   {
     "cortex-debug.enableTelemetry": false,
     "debug.showBreakpointsInOverviewRuler": true
   }
   ```

#### Проблема: "Status: No debugger attached"
**Симптомы:**
```
Status: No debugger attached
кнопок не вижу
```

**Решения:**
1. **Проверьте ST-Link подключение:**
   ```bash
   lsusb | grep ST-Link
   ```

2. **Проверьте питание STM32:**
   - LED на STM32 должен гореть
   - Проверьте USB питание или внешний источник

3. **Проверьте SWD пины:**
   - PA13 (SWDIO) и PA14 (SWCLK) должны быть подключены
   - GND должен быть общим

4. **Проверьте терминал OpenOCD:**
   - В VSCode: Terminal → gdb-server
   - Должны быть сообщения типа "Info : stm32f4x.cpu: hardware has 6 breakpoints"

5. **Если OpenOCD не запускается:**
   ```bash
   # Ручной запуск
   openocd -f openocd.cfg
   ```

6. **Проверьте конфигурацию:**
   ```bash
   cat openocd.cfg
   # Должно содержать правильные пути
   ```

7. **Используйте командную строку:**
   ```bash
   ./debug.sh  # Альтернативный способ
   ```

#### Проблема: "No device found"
**Решение:**
1. Подключите ST-Link
2. Проверьте питание STM32
3. Проверьте SWD пины (PA13, PA14)

#### Проблема: Отладка не запускается
**Решение:**
```bash
# Проверьте файлы
ls -la .vscode/
ls -la openocd.cfg
ls -la build/ILI9341_stm32f411.elf

# Перезагрузите VSCode
# Проверьте логи в Terminal > Output > Cortex Debug
```

### OpenOCD проблемы

**Симптомы:**
```
Error: open failed
```

**Решения:**
```bash
# Проверить ST-Link
lsusb | grep ST-Link

# Проверить права
sudo usermod -a -G plugdev $USER
sudo udevadm control --reload-rules

# Перезагрузить
sudo reboot
```

### GDB не подключается

**Симптомы:**
```
localhost:3333: Connection timed out
```

**Решения:**
```bash
# Проверить OpenOCD
ps aux | grep openocd

# Проверить порт
netstat -tlnp | grep 3333

# Запустить заново
pkill openocd
make debug &
sleep 2
arm-none-eabi-gdb build/ILI9341_stm32f411.elf
```

### Программа не останавливается на breakpoint

**Симптомы:**
```
Breakpoint not hit
```

**Решения:**
```gdb
# Проверить символы
file build/ILI9341_stm32f411.elf

# Проверить breakpoints
info breakpoints

# Установить заново
delete breakpoints
break main
```

### STM32 не отвечает

**Симптомы:**
```
Target not examined
```

**Решения:**
- Проверить питание STM32
- Проверить подключение SWD
- Проверить правильность openocd.cfg

### VSCode: "At least one OpenOCD Configuration File must be specified"

**Симптомы:**
```
At least one OpenOCD Configuration File must be specified
```

**Решения:**

1. **Исправить launch.json (вариант 1 - configFiles):**
   ```json
   "configFiles": [
       "${workspaceFolder}/openocd.cfg"
   ],
   "cwd": "${workspaceFolder}"
   ```

2. **Исправить launch.json (вариант 2 - serverArgs):**
   ```json
   "serverArgs": ["-f", "${workspaceFolder}/openocd.cfg"],
   "cwd": "${workspaceFolder}"
   ```

3. **Проверить файлы:**
   ```bash
   ls -la openocd.cfg
   ls -la .vscode/launch.json
   ls -la build/ILI9341_stm32f411.elf
   ```

4. **Перезагрузить VSCode:**
   - Ctrl+Shift+P → "Developer: Reload Window"

5. **Проверить OpenOCD:**
   ```bash
   which openocd
   openocd --version
   ```

6. **Использовать командную строку:**
   ```bash
   ./debug.sh  # Альтернативный способ отладки
   ```

7. **Проверить логи Cortex-Debug:**
   - View → Output → Cortex Debug

### VSCode: Cortex-Debug extension не установлен

**Симптомы:**
```
Type 'cortex-debug' not found
```

**Решения:**
```bash
# В VSCode
Ctrl+Shift+P → "Extensions: Install Extensions"
Искать: "Cortex-Debug"
Установить marus25.cortex-debug
```

### VSCode: Build task не найден

**Симптомы:**
```
Task 'Build' not found
```

**Решения:**
1. **Проверить .vscode/tasks.json**
2. **Перезагрузить VSCode**
3. **Создать tasks.json заново**

### OpenOCD: "Can't find a matching interface"

**Симптомы:**
```
Can't find a matching interface
```

**Решения:**
1. **Проверить ST-Link:**
   ```bash
   lsusb | grep ST-Link
   ```

2. **Исправить openocd.cfg:**
   ```cfg
   source [find interface/stlink.cfg]
   # или
   source [find interface/stlink-v2.cfg]
   ```

---

## 🔬 Продвинутая отладка

### Отладка FreeRTOS

```gdb
# Состояние задач
monitor rtos task-list

# Переключение задач
monitor rtos task-step

# Информация о задаче
monitor rtos task-info
```

### Memory анализ

```gdb
# Прочитать память
x/10xw 0x20000000    # RAM
x/10xw 0x08000000    # Flash

# Стек
info frame
backtrace full
```

### GPIO отладка

```gdb
# Чтение регистров GPIO
monitor mdw 0x40020400 10    # GPIOB registers

# Ручное управление пинами
monitor mwb 0x40020418 0xFF  # GPIOB->BSRR
```

### SPI отладка

```gdb
# SPI регистры
monitor mdw 0x40003800 10    # SPI2 registers

# Ручное управление SPI
monitor mww 0x4000380C 0x123 # SPI2->DR
```

### Watch expressions

В VSCode добавить в Watch:
- `hspi2.Instance->SR` - статус SPI2
- `GPIOB->IDR` - состояние пинов GPIOB
- `touch_initialized` - флаг инициализации

### Custom GDB commands

Создать `.gdbinit`:
```gdb
define reset_and_halt
  monitor reset halt
end

define show_regs
  info registers
end

define show_stack
  backtrace
end
```

### Логирование в файл

```gdb
# В GDB
set logging file debug.log
set logging on
set pagination off
```

---

## 📝 Диагностический чеклист

### Перед отладкой:
- [ ] ST-Link подключен и распознан
- [ ] OpenOCD установлен
- [ ] Проект собирается без ошибок
- [ ] ELF файл существует

### Во время отладки:
- [ ] OpenOCD запускается без ошибок
- [ ] GDB подключается к localhost:3333
- [ ] Breakpoint в main() срабатывает
- [ ] Программа выполняется до TOUCH_Init()

### Диагностика TOUCH_Init():
- [ ] Breakpoint в TOUCH_Init() установлен
- [ ] Пошаговое выполнение работает
- [ ] HAL функции не зависают
- [ ] Регистры в нормальном состоянии

### После диагностики:
- [ ] Причина зависания найдена
- [ ] Исправление протестировано
- [ ] Отладчик корректно завершен

---

## 🎯 Быстрые команды

### VSCode:
- **F5**: Запуск отладки
- **F10**: Step Over
- **F11**: Step Into
- **Shift+F11**: Step Out
- **Ctrl+Shift+F5**: Restart

### GDB:
```gdb
# Основные
r           # run
c           # continue
n           # next
s           # step
bt          # backtrace

# Breakpoints
b main      # break main
info b      # info breakpoints
d 1         # delete breakpoint 1

# Memory
x/10xw 0x20000000  # examine memory
p variable         # print variable
```

### OpenOCD (telnet):
```bash
reset halt      # сброс
halt           # остановка
resume         # продолжение
mdw addr len   # память слова
mwb addr val   # записать байт
```

---

## 📞 Поддержка

Если возникли проблемы с отладкой:

1. **Проверьте логи OpenOCD/GDB**
2. **Сравните с DEBUG_README.md**
3. **Проверьте подключение ST-Link**
4. **Переустановите OpenOCD**

## 🎉 Недавние проблемы и решения

### SPI тактирование тачскрина (MSP2807) - Решено ✅

#### Проблема
После добавления поддержки SPI2 для тачскрина MSP2807 система зависала в функции `TOUCH_Init()`. На логическом анализаторе не было видно сигналов SPI (SCK, MOSI, CS).

**Симптомы:**
- Система зависает после инициализации SPI2
- Нет CDC логов после `TOUCH: Initializing SPI2 for MSP2807`
- Логический анализатор показывает отсутствие SPI сигналов
- Команда `continue` в GDB не помогает - процессор зависает

#### Диагностика
1. **Проверка GPIO конфигурации:**
   ```gdb
   break TOUCH_Init
   continue
   # Программа останавливается в TOUCH_Init()
   ```

2. **Пошаговое выполнение:**
   ```gdb
   step
   # Выполняем строку за строкой
   ```

3. **Анализ регистров SPI2:**
   ```gdb
   # После HAL_SPI_Init()
   monitor mdw 0x40003800 10  # SPI2 регистры
   ```

4. **Проверка тактовой частоты:**
   - APB1 clock: 42MHz (для SPI2)
   - С prescaler 8: SPI clock = 42MHz / 8 = 5.25MHz
   - С prescaler 32: SPI clock = 42MHz / 32 = 1.3125MHz

#### Корень проблемы
**Слишком высокая скорость SPI для MSP2807.** Изначально использовался `SPI_BAUDRATEPRESCALER_8` (5.25MHz), что превышает максимальную частоту MSP2807 (~2MHz).

#### Решение
**Изменение BaudRatePrescaler с 8 на 32:**
```c
// В touch.c, TOUCH_Init()
hspi2.Init.BaudRatePrescaler = SPI_BAUDRATEPRESCALER_32;  // Было: 8
```

#### Результат
- SPI сигналы появились на логическом анализаторе
- Виден chip select (CS) и импульс MOSI
- Система больше не зависает в TOUCH_Init()
- CDC логи показывают успешную инициализацию

#### Уроки
1. **Всегда проверяйте datasheet устройства** - MSP2807 имеет ограничение по частоте SPI
2. **Начинайте с низких скоростей** при отладке новых устройств
3. **Используйте логический анализатор** для проверки аппаратных сигналов
4. **Документируйте изменения** - prescaler влияет на производительность

#### Текущее состояние
✅ TOUCH_Init() работает без зависаний
✅ SPI сигналы видны на анализаторе
✅ CDC логирование активно
✅ Система готова к подключению тачскрина

---

**Отладка STM32 - мощный инструмент для поиска и исправления сложных багов!** 🐛🔍
